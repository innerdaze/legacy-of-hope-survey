import { createAction } from 'redux-actions'

export const ready = createAction('APP/READY')
export const setPage = createAction('APP/SET_PAGE')
export const setLocale = createAction('APP/SET_LOCALE')
